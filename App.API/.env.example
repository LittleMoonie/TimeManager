# =============================================================================
# 🌍 GLOBAL PROJECT CONFIGURATION
# =============================================================================
# This file provides a template for the environment variables needed to run
# GoGoTime. Copy this to a .env file in the same directory for local
# development.

# -----------------------------------------------------------------------------
# 🚀 Core Application Settings
# -----------------------------------------------------------------------------

# Environment Type: Determines logging levels, error details, and optimizations.
# Options: development | staging | production
NODE_ENV=development
RUN_MIGRATIONS_ON_BOOT=true

# -----------------------------------------------------------------------------
# 🌐 Port Mapping
# -----------------------------------------------------------------------------
# Defines the external ports for accessing services from your local machine.

API_PORT=4000
WEB_PORT=3000
DB_PORT=5432

# -----------------------------------------------------------------------------
# 🗄️ PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# Connection details for the PostgreSQL database service.

DB_HOST=localhost
DB_USER=postgres
DB_PASS=password
DB_NAME=gogotime_dev
DB_SSL=false

# -----------------------------------------------------------------------------
# 🔐 Security & Authentication
# -----------------------------------------------------------------------------

# JWT Secret Key: A long, random, and secret string used to sign JWTs.
# 🚨 CRITICAL: CHANGE THIS IN PRODUCTION TO A SECURE 64+ CHARACTER RANDOM STRING.
# You can generate one with: openssl rand -hex 32
JWT_SECRET=supersecretkey-change-this-in-production-please

# -----------------------------------------------------------------------------
# 🔧 Docker Development Settings
# -----------------------------------------------------------------------------
# These settings improve file watching and hot-reloading performance within
# Docker, especially on Windows (WSL2) and macOS.

CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true