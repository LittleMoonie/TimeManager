# =============================================================================
# 🐳 DOCKER-COMPOSE ORCHESTRATION CONFIGURATION (App.Infra)
# =============================================================================
# This file contains variables needed by docker-compose itself for port
# mapping and service configuration.

# -----------------------------------------------------------------------------
# 🌐 Port Mapping
# -----------------------------------------------------------------------------
API_PORT=4000
WEB_PORT=3000
DB_PORT=5432

# -----------------------------------------------------------------------------
# 🗄️ Database Service Configuration
# -----------------------------------------------------------------------------
# These are used to initialize the 'db' service container.
DB_USER=postgres
DB_PASS=password
DB_NAME=gogotime_dev
